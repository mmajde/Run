<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="software.pama.run.tournament.TournamentActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start Tournament"
        android:id="@+id/startTournamentBtn"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:singleLine="true" />

    <Spinner
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/chooseDistanceSpinner"
        android:spinnerMode="dropdown"
        android:layout_toEndOf="@+id/textView3"
        android:layout_marginTop="59dp"
        android:layout_below="@+id/startTournamentBtn"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Choose distance"
        android:id="@+id/textView3"
        android:layout_toStartOf="@+id/chooseDistanceSpinner"
        android:layout_toLeftOf="@+id/chooseDistanceSpinner"
        android:layout_alignBottom="@+id/chooseDistanceSpinner"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/startTournamentBtn" />

    <TableLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@+id/chooseDistanceSpinner"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="98dp"></TableLayout>

    <TableLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@+id/chooseDistanceSpinner"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="52dp"></TableLayout>

</RelativeLayout>
